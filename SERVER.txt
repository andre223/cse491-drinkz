open folder server

run server.py to get host and port by running the command:
	python2.7 server.py

To Test the Server:
	run run-web in the /bin folder like this:


Click on 'WSGI SERVER TEST PAGE'

Enter the port number from running server.py and click submit

